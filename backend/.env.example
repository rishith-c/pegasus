# Pegasus backend (orchestrator) config
BACKEND_PORT=8001
SIGNAL_SERVICE_URL=http://localhost:8002
ML_SERVICE_URL=http://localhost:8003
DATABASE_URL=sqlite:///./pegasus.db
SERVICE_TIMEOUT=10.0

# Optional. Backend does not call Claude directly today; interventions come from
# the ML service. Present only so the file matches the team .env template.
ANTHROPIC_API_KEY=
